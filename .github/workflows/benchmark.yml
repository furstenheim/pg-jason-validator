name: Benchmark
on:
  workflow_dispatch:
    inputs:
      runs:
        description: 'timed runs per implementation'
        required: false
        default: '5'
      scale:
        description: 'copies of the dataset to load'
        required: false
        default: '1'
  push:
    # alternative trigger for tooling without actions:write permission:
    # committing a bump to this file starts a run with the default inputs
    branches: [master]
    paths: ['.github/benchmark-trigger']
jobs:
  benchmark:
    name: 🏁 tweet validation benchmark
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - name: Run benchmark
        env:
          RUNS: ${{ inputs.runs || '5' }}
          SCALE: ${{ inputs.scale || '1' }}
        run: tools/benchmark/run.sh | tee bench.log
      - name: Publish summary
        if: always()
        run: |
          {
            echo '## Tweet validation benchmark'
            echo '```'
            sed -n '/== benchmark:/,$p' bench.log
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
